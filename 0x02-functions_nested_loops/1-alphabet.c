/*
 * file: 1-alphabet.c
 * auth: PDT_Desekeye
 */
#include "main.h"
/**
 * main - entry point
 *
 * Return: Always 0 (epitychia)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);
	_putchar('\n')
		return (0);
}
